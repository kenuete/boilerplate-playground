import React from 'react'
import ReactDom from 'react-dom'
import {
  render,
  fireEvent,
  cleanup,
} from '@testing-library/react'

import App from './App'

describe('App', () => {
  test('', () => {
    expect(1).toBe(1)
  })
})
